spring.application.name=cloudfile-test-processor
spring.profiles.active=test

spring.security.oauth2.resourceserver.jwt.issuer-uri=testIdTokenIssuer
cognito.client-id=1234567890

security.oidc.enabled=true

management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

security:
  oidc:
    enabled: false